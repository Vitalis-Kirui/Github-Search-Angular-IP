import { StyleEffectDirective } from './style-effect.directive';

describe('StyleEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleEffectDirective();
    expect(directive).toBeTruthy();
  });
});
